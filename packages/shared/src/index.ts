/**
 * @serenity/shared
 *
 * Types and Zod schemas shared between apps/web and apps/api.
 * Phase 2+ will populate this with: user, mood entry, chat message,
 * journal envelope (encrypted), challenge, venting post.
 */

export const SHARED_VERSION = '0.1.0';
